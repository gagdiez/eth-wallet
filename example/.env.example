# Choose testnet (default) or mainnet. Restart the example server after changing.
VITE_NEAR_NETWORK=testnet

# Required when building the demo for deployment; optional for local development.
VITE_WALLET_MANIFEST_URL=https://your-wallet.example/manifest.json
